# Contacts mobile unit tests (Vitest)

```bash
cd modules/ContactsMobileWebclient/vue-mobile
npm install
npm run test:unit
```

Start with pure helpers (e.g. `getContactEmailsString`). Heavier parsers that need Core `src/`/`i18n` can be added later with mocks.
